<%= t 'devise.mailer.password_change.title' %>

===

<%= t 'devise.mailer.password_change.explanation_a', host: site_hostname %>

<%= t 'devise.mailer.password_change.explanation_b' %>

<%= t 'devise.mailer.password_change.explanation_c', host: site_hostname %>

<%= t 'devise.mailer.password_change.extra' %>
